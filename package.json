{
  "name": "activity-weather-ranking",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "schema": "node scripts/writeSchema.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "pnpm typecheck && pnpm test",
    "examples": "node scripts/writeExamples.ts"
  },
  "dependencies": {
    "@pothos/core": "^4.3.0",
    "graphql": "^16.9.0",
    "graphql-yoga": "^5.10.0",
    "mongodb": "^7.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "mongodb-memory-server": "^11.2.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
