# Krukit route log

Append-only. One line per Stage-0 routing decision. Telemetry only — nothing reads this for state.

`YYYY-MM-DD | task | route | reason`

2026-07-29 | Collinson take-home: rank next 7 days for skiing/surfing/outdoor/indoor sightseeing per city, Node+TS+GraphQL over Open-Meteo with persistence | full | Greenfield, but one service over 3-4 days — not "big greenfield" in the sense that triggers external-spec. The brief grades "how you worked" ABOVE the service, so the full stage chain (recon → grill → design → plan → act → verify → review) is itself the primary deliverable, and it lands in Git as it happens. User picked full over external-spec (spec-kit) and over fix.
